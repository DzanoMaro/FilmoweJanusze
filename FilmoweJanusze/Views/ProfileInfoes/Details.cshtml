@model FilmoweJanusze.ViewModels.ProfileInfoesDetails


@{
    ViewBag.Title = "Profil użytkownika " + Model.ProfileInfo.User.UserName;

    int tilesPerRow = 4;
    string coll_ratedmovies_in = null;

    if (Model.RatedMovies.Count() > 0)
    {
        coll_ratedmovies_in = "in";
    }
}

<!--NAGŁÓWEK-->
<div class="page-header well-jm realative">
    <div class="col-xs-10 col-sm-11">
        <h3>Profil użytkownika</h3>
        <h4>@Model.ProfileInfo.User.UserName</h4>
    </div>


    @if (Model.ProfileInfo.User.Id == ViewBag.CurrentUserID)
    {
        <div class="col-xs-2 col-sm-1">
            <button type="button" class="btn btn-default dropdown-toggle dropdownbutton margintop10" data-toggle="dropdown">
                <i class="glyphicon glyphicon-menu-down"></i>
            </button>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Edytuj profil", "Edit", new { id = Model.ProfileInfo.ProfileInfoID })</li>
                <li>@Html.ActionLink("Zarządzaj kontem", "Index", "Manage", null, null)</li>
            </ul>
        </div>
    }
       

        <div class="clearfix"></div>
    </div>
<!--INFORMACJE-->
<div>
    <div class="col-xs-2">
        <img class="img-circle img-thumbnail" src="@Model.ProfileInfo.PhotoURL" />
    </div>
    <div class="col-xs-4">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.ProfileInfo.FirstName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ProfileInfo.FirstName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ProfileInfo.LastName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ProfileInfo.LastName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ProfileInfo.Birthdate)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ProfileInfo.Birthdate)
            </dd>

        </dl>
    </div>
    <div class="clearfix"></div>
</div>

<!--OCENIONE FILMY-->
<div class="panel-group nopadding margintop20">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h4 class="panel-title">
                <a data-toggle="collapse" href="#coll_ratedmovies" class="panel-link">@Html.DisplayNameFor(model => model.RatedMovies)</a>
            </h4>
        </div>
        <div id="coll_ratedmovies" class="panel-collapse collapse @coll_ratedmovies_in">
            <div class="panel-body container-fluid nopadding">
                @if (Model.RatedMovies.Count() > 0)
                {
                    <div id="RatedMoviesCarousel" class="container-fluid nopadding carousel slide" data-ride="carousel">
                        <div class="carousel-inner">
                            <div class="item active">
                                @for (var i = 0; i < Model.RatedMovies.Count(); i++)
                                {
                                    if (i % tilesPerRow == 0 && i != 0)
                                    {
                                    @:</div><div class="item">
                                    }
                                    Html.RenderPartial("MoviePartial", Model.RatedMovies.ElementAt(i).Movie);
                                }
                            </div>
                        </div>

                        @if (Model.RatedMovies.Count() > tilesPerRow)
                        {
                            <!-- Left and right controls -->
                            <a class="left carousel-control width5perc" href="#RatedMoviesCarousel" data-slide="prev">
                                <span class="glyphicon glyphicon-chevron-left"></span>
                                <span class="sr-only">Previous</span>
                            </a>
                            <a class="right carousel-control width5perc" href="#RatedMoviesCarousel" data-slide="next">
                                <span class="glyphicon glyphicon-chevron-right"></span>
                                <span class="sr-only">Next</span>
                            </a>
                        }
                    </div>
                }
                else
                {
                    @:Nie oceniłeś jeszcze żadnego filmu :(
                }
            </div>
        </div>
    </div>
</div>


<!--OCENIONI AKTORZY-->
<div class="panel-group nopadding margintop20">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h4 class="panel-title">
                <a data-toggle="collapse" href="#coll_ratedmovies" class="panel-link">@Html.DisplayNameFor(model => model.RatedPeoples)</a>
            </h4>
        </div>
        <div id="coll_ratedmovies" class="panel-collapse collapse @coll_ratedmovies_in">
            <div class="panel-body container-fluid nopadding">
                @if (Model.RatedPeoples.Count() > 0)
                {
                    <div id="RatedPeopleCarousel" class="container-fluid nopadding carousel slide" data-ride="carousel">
                        <div class="carousel-inner">
                            <div class="item active">
                                @for (var i = 0; i < Model.RatedPeoples.Count(); i++)
                                {
                                    if (i % tilesPerRow == 0 && i != 0)
                                    {
                                    @:</div><div class="item">
                                    }
                                    Html.RenderPartial("PeoplePartial", Model.RatedPeoples.ElementAt(i).People);
                                }
                            </div>
                        </div>

                        @if (Model.RatedPeoples.Count() > tilesPerRow)
                        {
                            <!-- Left and right controls -->
                            <a class="left carousel-control width5perc" href="#RatedPeopleCarousel" data-slide="prev">
                                <span class="glyphicon glyphicon-chevron-left"></span>
                                <span class="sr-only">Previous</span>
                            </a>
                            <a class="right carousel-control width5perc" href="#RatedPeopleCarousel" data-slide="next">
                                <span class="glyphicon glyphicon-chevron-right"></span>
                                <span class="sr-only">Next</span>
                            </a>
                        }
                    </div>
                }
                else
                {
                    @:Nie oceniłeś jeszcze żadnych ludzi kina :(
                }
            </div>
        </div>
    </div>
</div>

